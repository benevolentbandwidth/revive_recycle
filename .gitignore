__pycache__/
*.pyc
.DS_Store
venv/

# Environment
.env*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Model weights (large binary files — download separately)
*.pt
*.pth
*.onnx

# Uploaded / processed images (not sample images in images/)
/tmp/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Node / Next.js (the web/ app also carries its own scoped .gitignore)
node_modules/
.next/
out/
*.tsbuildinfo
npm-debug.log*

# Firebase
# .firebaserc holds the real GCP project id and is not committed while infra is
# being provisioned (plan task 0.2). Copy .firebaserc.example to .firebaserc.
.firebaserc
.firebase/
firebase-debug.log*

# Issue drafts — working documents for filing GitHub issues from
# Implementation-Plan.md. Local scratch, not part of the build.
issues-*.md
